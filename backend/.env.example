# ---------------------------------------------------------------------------
# Creator AI Assistant — backend environment.
# Copy to `.env` and fill in. `.env` is git-ignored; never commit secrets.
# ---------------------------------------------------------------------------

# "local" (development) or "production" (EC2)
ENV=local

# Django
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_DEBUG=true
# Comma-separated. Local: localhost. Production: your EC2 DNS / domain.
ALLOWED_HOSTS=localhost,127.0.0.1
# Comma-separated frontend origins allowed to call the API.
# Local Vite dev server runs on :5173. Production: your deployed frontend origin.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# GitHub repo containing the Bodhisattva Challenge content (owner/repo-name).
# Files are fetched directly from GitHub — no local clone needed.
GITHUB_REPO=webuddhist/bodhisattvacharyavatara-rails
GITHUB_BRANCH=main
# Optional but recommended — raises GitHub API rate limit from 60 to 5000/hour.
# Create a fine-grained token at https://github.com/settings/tokens
# with "Public Repositories (read-only)" permission — no extra scopes needed.
# GITHUB_TOKEN=github_pat_...

# Google Gemini — one API key for both script generation and audio (TTS).
GEMINI_API_KEY=your-gemini-api-key
GEMINI_TEXT_MODEL=gemini-3.1-flash-lite
GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts
GEMINI_TTS_VOICE=Algenib
# Delivery direction prepended to the script for TTS. Leave unset to use the
# built-in calm, motivational, and clear default; override to tune the tone.
# GEMINI_TTS_STYLE=Narrate in a calm, warm, motivational voice — clear, gentle pacing.
